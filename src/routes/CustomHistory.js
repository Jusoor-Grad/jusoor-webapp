// Custom history object to allow navigation outside React components
export const history = {
  navigate: null, // Initialize with useNavigate()
  location: null, // Initialize with useLocation()
};
